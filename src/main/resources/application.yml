spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=s2l
#    jdbc:postgresql://localhost:5432/postgres?currentSchema=s2l
    username: postgres
    password: jerin123
    driver: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      database-platform: org.hibernate.dialect.postgreSQLDialect
      ddl-auto: update
  batch:
    initialize-schema: ALWAYS
    job:
      enabled: false



#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=s2l
#    username: postgres
#    password: jerin123
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#
